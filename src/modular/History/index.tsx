import * as React from 'react';
import { StyleSheet, View, Text } from 'react-native';
type Props = {}

const History = (props:Props) => {
    return (
        <View style={styles.screen}>
            <Text>inHistorydex</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    
screen:{}
});

export default History;